#!/bin/sh

set -e

case "$1" in
	install|upgrade)	
		for file in /etc/n4d/conf.d/OpenSysClone*.json; do if [-f $file ]; then rm $file;fi;done
	;;

	abort-upgrade|abort-remove|abort-deconfigure)
	;;
	
	*)
		exit 1
	;;
esac


